{
	"name": "@memory-game/database",
	"version": "1.0.0",
	"description": "",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"scripts": {
		"dev:migrate": "dotenv -e .env -- prisma migrate dev --name init && prisma generate",
		"prod:migrate": "prisma generate && prisma migrate deploy",
		"generate": "dotenv -e .env -- prisma generate",
		"migrate": "prisma migrate deploy && prisma generate",
		"build": "tsc",
		"dev": "tsc -w"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"@prisma/client": "^6.5.0",
		"@prisma/extension-accelerate": "^1.3.0",
		"dotenv": "^16.4.7"
	},
	"devDependencies": {
		"@types/node": "^20",
		"prisma": "^6.5.0",
		"typescript": "^5.8.2"
	},
	"prisma": {
		"schema": "./app/prisma/schema.prisma"
	}
}
